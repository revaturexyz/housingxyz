# tenantxyz :: docker-compose

networks:
  room_localhost:
services:
  web:
    build:
      context: ..
      dockerfile: .docker/dockerfile
    image: room
    depends_on:
      - data
    ports:
      - 10080:80
    networks:
      - room_localhost
    environment:
## TODO: Check connection string once created
      ConnectionStrings__TenantDb: 'Host=data;Database=tenant;Username=postgres;Password=Pass@word' 
  data:
    image: postgres:12.0
    networks:
      - room_localhost
    environment:
      POSTGRES_PASSWORD: 'Pass@word'
version: '3.7'