// nested results of the maps -> maps-details -> maps-geometry Google API result object
// lat and lng are the absolute latitude and longitude of the corresponding result
export interface MapsGeoLocation {
  lat: number;
  lng: number;
}
